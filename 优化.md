# SimPy Lab Studio 优化建议

**评估依据**：Schmitt 等，*LLM-driven discrete-event simulation: A generative AI framework for automated model generation, adaptation, and evaluation in manufacturing*，Journal of Manufacturing Systems 85 (2026) 642–661，DOI [10.1016/j.jmsy.2026.02.015](https://doi.org/10.1016/j.jmsy.2026.02.015)

**评估基线**：本仓库 `main` @ `acd5ac0`（发行版 1.0.0）

**文档性质**：差距分析 + 按性价比排序的优化路线。不含代码改动；文末给出参考实现与验收口径。

> **时效说明**：本文评估基于 1.0.0 源码（提交 `acd5ac0`）。此后上游已发布 v1.1.0 / v1.2.0（可视化建模编辑器、Windows 加密凭据存储、助手会话增强等），下文的"现状 / 缺口 / 完成度"未包含这些更新，实施前请按最新源码复核。

---

## 一、总体判断

同一份代码，用三个口径打分差别很大，先分清"要解决的是哪个问题"：

| 口径 | 完成度 | 依据 |
|---|---|---|
| 作为**可交付的桌面软件** | ~85% | 单文件 EXE + Inno Setup 中文安装包 + 便携 ZIP；232 项测试通过；`docs/release-validation.md` 记录了真机安装/卸载验证（含用户数据 SHA-256 不变） |
| 作为**论文那套端到端 LLM 工作流** | ~40% | 论文的六个智能体只落地了"参数级适配 + 约束校验"；生成、检查、可视化、评估器、结构级适配均缺失 |
| 作为**案例 A 的单案例闭环** | ~80% | 参数、CPD、瓶颈排序、重复实验、假设清单、中文对照报告齐全，数值方向与论文一致 |

**一句话结论**：现在做的是"论文里最稳的那一环"的产品化，而论文真正想验证的那一环——让 LLM 写可执行模型——被绕开了。这不是缺陷本身，但对外不能说"复现了论文"。

---

## 二、逐环节对照

| 论文环节 | 论文做法 | 现状 | 完成度 |
|---|---|---|---|
| 4.3.1 输入数据分析 | 事件日志 → CT/可用率/MTTR；PM4Py 提取零件流 | `mining.py`（XES/JSONL → 分布/容量估计，`simlab mine`）；案例 A 直接用论文表 4 数值 | ~60% |
| 4.3.2 模型生成 | builder-agent 按蓝图 one-shot **写 SimPy 代码**，inspector 自动修错 | `design.py`：自然语言 → 经 Pydantic 校验的 **YAML 蓝图**，单次调用、无自动修复、不产代码；studio 无生成能力 | ~25% |
| 4.3.3 模型适配 | BT 分析 → 建议 → CPD 校验 → adaptor **写代码**实施三级复杂度 | 利用率排序当"瓶颈代理指标" + `validate_case_a_adaptation` 做 CPD + `studio_ai.py` 白名单参数修改 | ~40%（参数级≈100%，结构级/逻辑级 0%） |
| 4.3.4 模型评估 | 重复实验图表 + evaluator-agent **文本权衡与下一步建议** | CRN 配对重复、均值+置信区间、`comparison.md`、`evaluation` 块——**全是确定性代码**；`ai.py` 的 LLM 解读只挂在旧 `analyze` 命令上，studio 未接入 | ~55% |
| 4.1.3 人在回路 | 3 个强制/可选检查点 | studio：校验通过 → 自动建新版本 → 跑预览，**前端 0 处"审批"字样**，靠人事后比对版本；严格 `propose→approve/reject→apply`+审计在旧栈（`workflow.py`/`service.py`），面向 `ProjectConfig`，与制造引擎没打通 | ~50% |
| 5.3 / 5.4 案例覆盖 | A（4 机串联）+ B（7 工位、并行压机、8.9% 缺陷、结构性建议） | 案例 A 完整；`defect`/`parallel`/`splitter`/`merger`/`case_b` 全库 **0 命中** | A ~80%，B **0%** |
| 4.1.1 多模型分工 | GPT-5.1 生成 / 5-mini 适配 / 4o 结构化 | `studio_profiles.py` 多套连接保存切换 + 每版本审计实际模型（比论文细），但一次只激活一个，无按任务自动分流 | ~50% |
| 5.6 交叉验证 | 与 FACTS Analyzer 对比，5–10% 阈值 | 独立实现 + 手算单测 + 假设清单；TH 7.71 vs 论文 7.38、SEC 13.91 vs 14.22，方向一致，但未把 5–10% 当验收线 | ~50% |

---

## 三、已经比论文做得好的地方（保持，不要回退）

1. **产品化程度**：pywebview 独立窗口、PyInstaller 单文件、Inno Setup 中文向导、WebView2 检测、单实例、`%LOCALAPPDATA%` 数据目录、卸载保留实验数据、退出清理子进程。论文只有脚本 + GitHub。
2. **治理与安全**：密钥只留进程内存、base_url 只允许 HTTPS/本机、路径白名单而非 `eval`、工作量上限、密钥回显脱敏、幂等 + 乐观锁的审批状态机、审计序列、MCP「仅提案」子集。
3. **可复现纪律**：多条实现假设写入每份 `results.json`、CRN 共同随机数、多进程结果一致、手算单测、发行验证覆盖安装卸载。
4. **2D 状态回放 + 版本恢复 + 实验 ZIP/HTML 导出**：论文完全没有的产品能力。

---

## 四、缺口（按对论文主张的杀伤力排序）

1. **不生成代码**。论文的论点是"LLM 产出可执行 SimPy 模型"，现在是"LLM 只填白名单参数"。这躲开了论文最脏的环节（案例 A 10 次里 1 次崩溃、案例 B 11 次里 4 次需人工修），但也意味着论文的风险—收益结论无法验证。
2. **只有串联案例 A**。论文 6.2 明确说复杂度一升准确率就掉，而结构级适配（加第三个并行工位）——论文最亮的 demo——完全没有。
3. **缺可视化与评估器智能体**。现在是数字+图表，论文是"决策建议+权衡取舍"。`ai.py` 里现成的结构化解读组件没接进 studio，这是最便宜的一个洞。
4. **两套栈并存**：旧 `ProjectConfig` 服务系统（审批/MCP/API/analyze/design/mine）与新 `ManufacturingConfig` 制造引擎（studio/case-a）互不相通，治理能力恰好缺席在最需要它的那一半。
5. **没有 LLM 正确率度量**。论文用 21 次实验给出一次通过率与错误类型分布（Fig. 18）；现有 LLM 测试用注入式假客户端，真实模型的通过率/幻觉分类没有数据。
6. **工程收尾**：无 CI（无覆盖率门槛）；发行名 `simpy-kpi-lab 0.3.0` 与产品名 1.0.0 不一致；安装包未签名；Windows 10 未真机复测。

---

## 五、优化路线（按性价比从高到低）

### 1. 评估器智能体接入 studio（最便宜、最贴论文）

- **改法**：新增 `studio_eval.py`；`POST /sessions/{id}/evaluate`，把已完成重复实验的均值/置信区间交给模型出中文结论；结论必须通过**方案名白名单**校验（引用本地不存在的方案即拒绝且不落库）；本地先算 Pareto 前沿与逐指标最优作为事实底座；结论记录所用模型，随导出写 `evaluations/*.md` 并渲染进 `report.html`；前端在「方案与评估」面板加按钮，并说明禁用原因（无密钥 / 少于两个方案）。
- **验收**：注入式端到端测试；引用不存在方案 → 422 且不落库；无密钥 → 503 而非崩溃；导出包含结论；密钥不出现在任何落盘文件里。

### 2. 案例 B（七工位、并行压机、分流/合流、缺陷率与缺陷汇、结构性适配）

- **改法**：`MachineSpec` 增加显式 `input` / `outputs`（不填则保持串联默认语义，向后兼容）、`routing`（round_robin / shortest_queue）、`defect_rate`；计数器增加 defects（缺陷件不计入 TH 与 SEC 分母，单独报告缺陷率）；`case_b_config()` 复现论文表 6；`validate_case_b_adaptation()` 实现两条 CPD（压机 CT ≥ 60 s、缓冲区容量保持原值）；`case_b_scenarios()` 给出 V1（违规诊断）/V2/V3/V4（V4 = 增加第三台压机）。
- **验收**：与论文表 7 基线对照（TH 27.29 件/h、WIP 18.32 件、SEC 0.4506 kWh/件，允许 5–10% 偏差）；物料守恒误差为 0；实测缺陷率 ≈ 8.9%；瓶颈顺序 Press2 → Press1 → Quality 与论文一致；V4 相对基线明显提升 TH。

### 3. 确定性流程图（visualizer）

- **改法**：新增 `visualize.py`，由配置直接推导拓扑，输出 Mermaid 文本 + 独立 SVG（论文图 12 的等价物），再加 `simlab visualize --case a|b`。**SVG 必须用 presentation 属性而不是内部 CSS**，否则 MuPDF/Word 会渲染成黑块。
- **验收**：SVG 可被 XML 解析且包含全部节点与 50/50、8.9% 标注；同一配置两次渲染逐字节一致；案例 B 三压机版本能正确多出一条分支。

### 4. 代码生成沙箱支线（builder / inspector）

- **改法**：新增 `codesandbox.py`。契约：模型输出 `simulate(parameters, seed)` 返回 `{completed, energy_kwh, avg_wip}`。三层约束：① AST 白名单（导入仅限 simpy/math/random 等；禁 exec/eval/open/getattr/双下划线属性/with/global；限制体积）② `python -I -P` 隔离子进程（最小环境变量、临时工作目录、超时、结果写文件而非 stdout）③ 与本地引擎同种子对照 KPI 量级。产物 `generated_model.py` + `report.json/md`，**永不自动生效**。
- **验收**：每类静态违规一条测试；超时用例；子进程读不到父进程密钥；端到端（假 agent）跑通；文档明确"这是防误执行，不是对抗恶意代码的安全边界"。

### 5. 审批/审计/MCP 与制造引擎打通

- **改法**：新增 `manufacturing_control.py`：`pending → approved/rejected → applied/failed`；允许清单由当前配置生成并在论文模式下标注 CPD 保护项；应用前重跑配置校验 + CPD（违规 → `failed` 并记录原因，配置不变）；幂等 `operation_id`（**判断要放在乐观锁之前**，否则重试会误报版本过期）；`expected_version` 乐观锁；JSON 持久化 + 单调审计；MCP 只暴露 `get_session / get_allowed_actions / submit_proposal / list_events`；CLI `simlab control new|propose|decide|run|show`。
- **验收**：CPD 违规提案变 `failed` 且版本列表不变；重复 `operation_id` 返回同一提案；MCP 工具面断言无 approve/reject/run；CLI 全流程测试。

### 6. 真实 LLM 通过率基准

- **改法**：新增 `benchmark.py` + `simlab benchmark --yes`：固定四类任务（参数级 / CPD 违规 / 结构性拒绝 / 开放式）× N 次真实调用；结果分六类（通过校验、无改动、被策略拒绝、输出错误、配置失败、冒烟仿真失败）；对"通过校验"的结果做 2 天冒烟仿真 + 物料守恒检查；输出 `results.json` 与中文 `benchmark.md`。**会消耗 API 额度，必须显式确认。**
- **验收**：注入式测试覆盖分类与报告渲染；带密钥跑一次得到真实一次通过率与失败详情。

### 7. 工程收尾

- **改法**：`.github/workflows/ci.yml`（Windows，Python 3.11 + 3.13，ruff + pytest + `--cov-fail-under=80`）；发行名统一为 `simpy-lab-studio` 并让 `pyproject.toml` / `tools/desktop_version.txt` / `installer/studio.iss` 版本一致（`git tag` 与 Releases 同步）；`.gitignore` 补本地产物（`/SimPy Lab Studio/`、安装包）；重建 EXE 与安装包。
- **验收**：CI 绿；本地全量测试通过且覆盖率 ≥ 80%；EXE 与安装包版本资源一致；用 Inno Setup 6.7+ 完整走一遍打包流程。

---

## 六、建议的验收口径

| 对象 | 口径 | 建议阈值 |
|---|---|---|
| 案例 A | 与论文表 5 / 图 6 对照（TH、WIP、SEC） | 偏差 ≤ 10%（当前独立实现：TH +4.4%、WIP −7.4%、SEC −2.1%） |
| 案例 B | 与论文表 7 基线对照 | 偏差 ≤ 10%；瓶颈顺序一致；缺陷率 ≈ 8.9% |
| 仿真正确性 | 物料守恒、缓冲容量、实体数 | 误差必须为 0，作为回归门禁 |
| LLM 可靠性 | 一次通过率 + 错误类型分布（论文表 5/7、图 18 口径） | 记录基线并逐版本对比，不设绝对门槛 |
| 工程 | 测试数、覆盖率、ruff | 覆盖率 ≥ 80% 且不低于上一版 |

---

## 七、参考实现（可恢复）

上述 1–7 项在上一轮已经全部实现并验证过（310 项测试通过、覆盖率 85.7%、案例 B 与论文基线偏差 ≤ 5%），代码以标签形式保存在本仓库：

```text
git switch -c feat/paper-parity archive/paper-parity-1.1.0     # 恢复完整实现
git show archive/paper-parity-1.1.0 --stat | head -40          # 只看改动清单
git cherry-pick <commit>                                        # 按单项挑选
```

六个提交依次为：

| 提交 | 内容 |
|---|---|
| `d1628fa` | 评估器智能体（路线 1） |
| `aca4bf9` | 案例 B + 流程图（路线 2、3） |
| `a5be8e5` | CI + 覆盖率门槛 + 发行名/版本（路线 7） |
| `8349aaf` | 真实 LLM 基准（路线 6） |
| `7c15f68` | 代码生成沙箱（路线 4） |
| `bd453a6` | 审批/审计/MCP 治理面（路线 5） |

已构建的 1.1.0 产物仍在 `dist/`：`SimPy Lab Studio.exe`（20.5 MB，SHA-256 `4ed04445…cde888`）、`SimPy-Lab-Studio-Setup-1.1.0.exe`（23.72 MB，SHA-256 `3bcf0371…dbd294d`）。

---

## 八、实施时容易踩的坑

1. **包名/版本一致性**：发行名、`__init__.py` 的版本解析、`tools/desktop_version.txt`、`installer/studio.iss` 四处必须同步，否则 EXE 属性与实际版本对不上。
2. **SVG 不要用内部 CSS**：MuPDF、Word、部分看图工具会忽略 `<style>`，图形全部渲染成黑色实心块。
3. **冻结包体积受构建环境影响**：只装运行依赖时 20.5 MB，把整个 dev 环境装进去会涨到 34 MB（dev 依赖虽被 spec 排除，但它们的许可证文本会被打进 `licenses.html`）。
4. **MCP / pywebview 是可选依赖**：导入要延迟到函数内，否则缺依赖时整个模块不可用。
5. **幂等要放在乐观锁之前**：否则"提交成功但响应丢失"的重试会被误判为版本过期。
6. **案例 B 的分流语义要写进假设**：论文图 12 标注 50/50 轮询；最短队列是另一种合理策略，切换时必须在结果里标记，否则两次实验不可比。
7. **桌面界面目前只支持串联线**：导入带分流/合流的模型应明确拒绝并指向命令行，而不是画一条错的动画。

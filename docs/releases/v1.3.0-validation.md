# v1.3.0 发布验证

验证日期：2026-09-17。实际执行环境：Windows 11 x64、Python 3.13、Edge / WebView2。以下是本次执行结果，不代表已在所有 Windows 版本、显卡和外部 AI 服务上测试。

| 范围 | 结果 |
| --- | --- |
| 全量程序回归 | 304 项通过；随后扩展队列取消/重启边界，扩展模块 36 项全部通过（包含新增 2 项） |
| 图形模型算法 | 6 项通过，串联连接、分支拒绝、参数及删除边界保持一致 |
| 浏览器交互 | 工作台、设备拓扑、图形编辑、历史评估、助手对话、密钥保存、新实验工具，7 组全部通过 |
| 打包内容 | 85 个静态资源逐字节核对；9 个相关模块编译内容与源码一致 |
| EXE 自检 | 内置资源、自动后台服务、真实预览、XLSX 读取、草稿、备份、加密密钥重启恢复和移除通过 |
| 原生桌面 | 播放、暂停、时间轴、真实完整评估、缓冲热力图、备份、模型配置、图形编辑和截图验证通过 |
| 安装程序 | 全新安装、重复安装、1.2.0 → 1.3.0 升级、快捷方式、卸载登记及卸载通过 |
| 用户数据 | 安装与卸载前后 38 个现有用户 JSON 文件的数量和 SHA-256 保持一致 |

最终应用 EXE SHA-256：

```text
3918c3d49711867552601c346fa778022818072845b3985470fb4accb154857d
```

最终安装程序 SHA-256：

```text
5492b79caddf8ca628dace24fb1a6f630185e2123e86b45bae488d4bbf4c199f
```

原有 SimPy 制造仿真、预览引擎和 KPI 聚合源码未改动。新功能读取原有结果，并通过原有模型约束创建新方案。测试服务使用隔离数据目录和注入 AI 响应，不调用用户真实 API，也不打包用户实验或密钥。

验证工具为 `tests/test_studio_tools.py`、`tools/check_priority_browser.cjs`、原有 `tools/check_*_browser.cjs`、`tools/check_model_graph.cjs`、EXE 的开发自检参数和 `tools/check_installer.ps1`。当前依赖会产生一条 Starlette / AnyIO 弃用提示，不影响本次检查结果。
